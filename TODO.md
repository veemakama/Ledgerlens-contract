- [x] Add `# Examples` doc blocks to the listed public functions in `contracts/ledgerlens-score/src/lib.rs`.
- [ ] Ensure each example creates `Env`, registers contract, creates client, calls `initialize`, calls the documented function, and asserts expected result.
- [ ] Run tests/doc tests for `contracts/ledgerlens-score` via cargo.



